name: Deployment
on:
  push:
    branches:
      - master
      - dev
env:
  MONGODB_DB_NAME: GHAEnv

jobs:
  test:
    env:
      MONGODB_CLUSTER_ADDRESS: ghaenv.829lcsa.mongodb.net
      MONGODB_USERNAME: obulesucloud_db_user
      MONGODB_PASSWORD: JSwepfGJKo4gSU01
      PORT: 
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run Server
        run: npm start & npx wait-on http://127.0.0.1:XYZ
      - name: Run tests
        run: npm test
      - name: Output Information
        run: echo "...."
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: |
          echo "..."

